name: Build Preview

on:
  push:
    branches: [main]
  workflow_dispatch: {}

jobs:
  build_android:
    name: Build Android
    type: build
    params:
      platform: android
      profile: preview
